# Interface with OpenFermion

Struqture also has an interface with the [OpenFermion](https://quantumai.google/openfermion) package allowing users to switch from one package to the other.

For now only the interface for `PauliHamiltonian` is implemented with the functions `struqture_to_openfermion` and `openfermion_to_struqture`.

